#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import argparse
import json
import os
import sys
import time
import uuid
import psycopg2

from typing import Dict, Any, Optional, List

from copaw_client import CoPawClient, CoPawConfig

# python info_extractor.py --copaw-url "http://192.168.100.23:8081" --shard-index 1 --shard-total 5


# 使用 CoPaw v0.1.0.post1
PROMPT_TEMPLATE = r"""
对 __Doctor_URL__ 进行提取，**只提取该 URL 对应的目标医生的详细信息**，经过结构化字段提取后，将结果推送到 RabbitMQ 队列。

**极其重要：只处理当前页面的目标医生，严禁处理页面上出现的其他医生！**
- ❌ 禁止提取：推荐医生、相关医生、同科室医生、热门医生、猜你喜欢等模块中的医生
- ✅ 只提取：当前 URL 对应的唯一目标医生（页面主体内容展示的医生）

**识别目标医生的方法：**
- 页面标题中的医生姓名
- URL 中包含的医生 ID 或姓名
- 页面主体内容（main content）中展示的医生
-  breadcrumbs/导航路径 中指向的医生

---

## 一、执行步骤（按顺序执行）

### 1. 访问目标 URL 并识别目标医生
- 访问指定的医生详情页 URL
- **确认当前页面类型**：必须是医生详情页，而非列表页
- **识别目标医生**：
  - 查看页面标题（title 标签），提取医生姓名
  - 查看 URL 结构，确认医生标识（ID 或姓名拼音）
  - 定位页面主体内容区域（通常是 `<main>`, `<div class="doctor-content">`, `<div class="detail-content">` 等）
- **排除其他医生信息**：
  - 忽略"推荐专家"、"相关医生"、"同科室医生"、"热门医生"等侧边栏/底部模块
  - 忽略"医生团队"、"专家团队"等列表式展示
  - 忽略"您可能还想看"、"猜你喜欢"等推荐模块
  - 忽略页眉/页脚中的医生链接

### 2. 验证目标医生身份
**关键验证步骤（必须执行）：**
- 确认页面只提取 **一个医生** 的信息
- 核对页面主体内容的医生姓名与页面标题一致
- 如果页面包含多个医生信息，**只提取 URL 对应的那个医生**
- 如果无法确定哪个是目标医生，输出警告并跳过

**典型页面结构分析：**
```html
<!-- 目标医生区域（要提取） -->
<main class="doctor-detail">
    <h1>张三 主任医师</h1>
    <div class="info">...</div>
</main>

<!-- 推荐医生区域（忽略） -->
<aside class="recommend-doctors">
    <div class="doctor-card">李四...</div>
    <div class="doctor-card">王五...</div>
</aside>

<!-- 底部推荐（忽略） -->
<div class="related-doctors">
    <div class="doctor-item">赵六...</div>
</div>
```

### 3. 提取目标医生详细信息

**只从目标医生所在区域提取以下字段：**
- name: 医生姓名（必须是页面主体的目标医生）
- hospital: 医院全称（**优先从网页源码的 `<title>` 标签中提取**，格式通常为"医生姓名 - 科室 - 医院名称"或"医院名称 - 医生姓名"，从中解析出医院全称；如果 title 中没有，再从页面内容中提取）
- standard_department: 标准科室名
- display_department: 展示科室名
- title: 职称（**必须从标准值中选择**：`主任医师 `、`副主任医师 `、`主治医师`、` 主管医师`、`医师`、`医士`、`住院医师`、`助理医师`、`主任药师 `、`副主任药师 `、`主管药师`、`药师`、`药士`、`主任护师 `、`副主任护师 `、`主管护师`、`护师`、`护士`、` 主任技师`、`副主任技师 `、`主管技师`、` 技师`、` 技士`、`主任麻醉师 `、` 副主任麻醉师`、` 主管麻醉师`、` 麻醉师`、`主任检验技师 `、`副主任检验技师`、`检验技师`、`检验技士`、`主管检验技师`、`主任康复师 `、`副主任康复师 `、`主管康复师`、` 康复师`、`康复治疗师 `、` 心理咨询师`、` 主任检验医师`、` 副主任检验医师`、` 主治检验医师`、` 检验医师`、` 主管检验医师`、` 高级技师`、` 高级按摩师`、`中级按摩师 `、` 初级按摩师`、` 初级营养师`、` 主管营养师`、` 副主任营养师`、` 主任营养师`、`主任中医师 `、` 副主任中医师`、` 主治中医师`、` 主管中医师`、` 中医师`、`中医士`、` 住院中医师`、`主任中药师 `、`副主任中药师`、` 主管中药师`、` 中药师`、` 中药士`、` 主管药剂师`、`主任药剂师 `、` 副主任药剂师`）
- administrative_position: 行政职务（**必须从以下值中选择**：`"书记"`、`"院长"`、`"局长"`、`"所长"`、`"经理"`、`"站长"`、`"主任"`、`"科长"`、`"处长"`、`"主管"`、`"组长"`、`"病区护士长"`、`"科护士长"`、`"护士长"`、`"住院部医生"`、`"助理"`、`"秘书"`、`"采购"`、`"库管"`、`"普通职员"`、`"地区分公司"`、`"副书记"`、`"副院长"`、`"副局长"`、`"副所长"`、`"副经理"`、`"副站长"`、`"副主任"`、`"副科长"`、`"副处长"`、`"病区副护士长"`、`"科副护士长"`、`"进修护师"`、`"总住院医师"`、`"护理部副主任"`、`"负责人"`、`"支部书记"`、`"主任助理"`、`"带组医生"`、`"所有权"`、`"办公室主任"`、`"床位医生"`、`"进修医生"`、`"院长助理"`、`"进修护士"`、`"副护士长"`）
  - ⚠️ **重要：只提取医生在当前医院（即 `hospital` 字段对应的医院）担任的行政职务**
  - ⚠️ **重要：当有多个职务时，优先提取当前医院的职务，排除在其他医院/机构的职务**
  - ⚠️ **识别规则**：
    - ✅ 有效：`"xx 医院护士长"`、`"xx 医院心内科主任"`、`"本院护理部副主任"`、`"检验科 xx 组组长"`、`"xx 科室组长"`
    - ✅ 有效：`"xx 科护士长"`、`"xxICU 护士长"`、`"EICU 护士长"`、`"NICU 护士长"`（科室 + 护士长格式）
    - ❌ 无效：`"中华医学会 xx 分会委员"`（学术组织）、`"曾任 xx 医院院长"`（历史职务）、`"xx 大学医学院院长"`（非本医院）、`"xx 医院（分院/帮扶医院）主任"`（非本院）
    - ❌ **无效：科研机构职务（如"xx 研究所所长"、"xx 研究中心主任"、"xx 实验室主任"等）→ 返回 null**
  - ❌ **禁止提取**：在其他医院或机构担任的职务、学术组织的职务（如学会委员、杂志编委等）、历史上的职务、**科研机构职务**
  - 📌 **示例**：
    - `"现任综合临床片科护士长/广东省人民医院河源医院护理部主任"` → `"护士长"`（当前医院职务）
    - `"检验科流式细胞组组长"` → `"组长"`（科室内部行政职务）
    - `"广东省人民医院 EICU 护士长"` → `"护士长"`（科室 + 护士长格式）
    - `"xx 医院院长，兼任 xx 大学医学院教授"` → `"院长"`（排除学术职务）
    - `"广东省神经科学研究所所长"` → `null`（科研机构职务，非医院行政职务）
- academic_title: 学术头衔（**必须从以下值中选择**：`"教授"`、`"副教授"`、`"讲师"`、`"助教"`、`"研究员"`、`"副研究员"`、`"助理研究员"`、`"研究实习员"`）
- education: 学历（**必须从以下值中选择**：`"博士研究生"`、`"硕士研究生"`、`"本科"`、`"大专"`、`"中专"`、`"高中"`、`"初中及以下"`）
   - 🎓 **重要：只提取最高学历**
   - ⚠️ **重要：只有在页面明确显示学历信息时才提取，如果页面没有提到学历，返回 null**
   - ❌ **禁止推测**：不要根据职称、毕业院校等信息推测学历
   - ❌ **禁止默认值**：没有明确学历信息时，绝对不要用"本科"作为默认值
   - ✅ **正确做法**：没有找到学历信息 → `education: null`
- degree: 学位
- alma_mater: 毕业院校（**只提取学校名称，不包含院系/专业，不包含附属机构**）
   - 🎓 **重要：只提取获得最高学历的学校**
   - 🎓 **重要：当页面显示多个毕业院校时，需要根据学历层次判断**
   - 🎓 **判断规则**：
     - 如果有明确学历信息（如"本科毕业于 A，博士毕业于 B"），选择最高学历对应的学校
     - 如果只说"先后毕业于 A，B"没有学历层次，**选择最后一个学校**（通常是最高学历）
     - 示例："先后毕业于华中科技大学，中山大学" → `"中山大学"`
   - 🎓 **学历层次优先级**：博士 > 硕士 > 本科 > 大专 > 中专
     - **只计算正式的学历教育（学士、硕士、博士）**，不包括进修、培训、研修等
     - **如果最高学历是在医院（如"xx 附属医院"）获得的，而较低学历是在大学获得的，优先选择大学**
     - 示例："中国医科大学学士学位，中山大学附属孙逸仙纪念医院硕士学位" → `"中国医科大学"`（学士学位是学校教育）
   - 🎓 **博士后/访问学者/进修处理（极其重要）**：
     - ❌ **禁止**：仅因页面提到"XX 大学博士后"、"XX 大学访问学者"、"XX 大学进修"就提取该大学
     - ❌ **禁止**：将"留学"、"培训"、"研修"的机构当作毕业院校
     - ✅ **只有**页面明确提到"毕业于 XX 大学"、"XX 大学毕业"、"获得 XX 大学学位"时才能提取
     - 示例："美国俄亥俄州立大学博士后" → `null`（博士后不是学历教育）
     - 示例："哈佛大学访问学者" → `null`（访问学者不是学历教育）
     - 示例："毕业于中山大学，获博士学位" → `"中山大学"`
   - 🏥 **重要：如果学校名称包含"大学 + 医学院/附属医院"，只提取大学名称**
   - ✅ 正确：`"蚌埠医学院"`、`"北京大学"`、`"华中科技大学"`、`"复旦大学"`、`"中山大学"`、`"美国俄亥俄州立大学"`、`"中国医科大学"`
   - ❌ 错误：`"蚌埠医学院临床医学系"`、`"北京大学医学部临床专业"`、`"华中科技大学同济医学院"`、`"复旦大学附属医院"`、`"中山大学附属孙逸仙纪念医院"`
- years_of_experience: 从业年限（**提取数字，如"从事超声诊断工作二十年" → 20**）
  - 💼 **重要：从简介/擅长描述中提取工作年限**
  - 💼 **支持中文数字转换**：`"二十年"` → `20`，`"三十年"` → `30`
  - 💼 **示例**：`"从事神经科临床工作 20 余年"` → `20`，`"从业 30 年"` → `30`，`"工作二十五年"` → `25`
  - 🧮 **重要：如果提供的是参加工作的年份，需要计算从业年限**
  - 🧮 **计算方法**：用当前年份（2026）减去参加工作年份
  - 🧮 **示例**：`"1999 年参加工作"` → `2026 - 1999 = 27`，`"2000 年毕业"` → `2026 - 2000 = 26`
- intro: 医生简介原文
- specialty: 擅长领域原文
   - 📝 **重要规则：必须提取完整的简介和擅长内容**
   - 📝 **简介（intro）**：包含医生的教育背景、工作经历、学术任职、科研成果等
   - 📝 **擅长（specialty）**：包含医生的专业领域、诊疗范围、技术特长等
   - 📝 **提取规则**：
     - **必须提取全部内容**，不要省略、不要摘要
     - **保留原文格式和标点符号**
     - **如果页面有明确的"简介"、"擅长"标签，提取对应内容**
     - **如果没有明确标签，从页面主体内容中提取相关描述**
     - **包含所有细节**：学术任职、科研成果、专利、论文、培训经历等都应包含在简介中
   - 📌 **示例**：
     - 简介：`"肿瘤学博士、美国访问学者、研究生导师。美国妇产科腹腔镜协会会员、全国单孔腹腔镜手术专家组成员...主持和参与多项科研项目，发表 SCI 多篇，授权专利 2 项。"`
     - 擅长：`"专攻子宫颈肿瘤、子宫内膜肿瘤、卵巢肿瘤等妇科恶性肿瘤和子宫粘连等女性生殖道损伤修复。擅长妇科肿瘤手术、腹腔镜及宫腔镜微创手术和阴式手术..."`

**提取时的重要规则：**
1. 只从页面主体的目标医生区域提取
2. 忽略侧边栏、底部、推荐区域中的任何医生信息
3. 如果页面包含多个医生卡片，只提取与 URL 对应的那个
4. 字段缺失时用 null 填充，不要编造数据
5. **alma_mater（毕业院校）只提取学校名称，必须剥离院係/专业信息和附属机构**
   - ✅ 正确：`"蚌埠医学院"`、`"北京大学"`、`"复旦大学"`、`"华中科技大学"`、`"中山大学"`、`"美国俄亥俄州立大学"`、`"中国医科大学"`
   - ❌ 错误：`"蚌埠医学院临床医学系"`、`"北京大学医学部临床专业"`、`"华中科技大学同济医学院"`、`"中山大学附属孙逸仙纪念医院"`
   - 🎓 **重要规则**：
     - 只提取获得最高学历的学校
     - **当页面显示多个毕业院校时（如"先后毕业于 A，B"）**：
       - 如果有明确学历层次（如"本科毕业于 A，博士毕业于 B"），选择最高学历对应的学校
       - 如果没有学历层次信息，**选择最后一个学校**（通常是最高学历）
       - 示例："先后毕业于华中科技大学，中山大学" → `"中山大学"`
     - **学历层次优先级**：博士 > 硕士 > 本科 > 大专 > 中专
       - **只计算正式的学历教育（学士、硕士、博士）**，不包括进修、培训、研修等
       - **如果最高学历是在医院（如"xx 附属医院"）获得的，而较低学历是在大学获得的，优先选择大学**
       - 示例："中国医科大学学士学位，中山大学附属孙逸仙纪念医院硕士学位" → `"中国医科大学"`
     - ❌ **博士后/访问学者/进修处理（极其重要）**：
       - **禁止**：仅因页面提到"XX 大学博士后"、"XX 大学访问学者"、"XX 大学进修"就提取该大学
       - **禁止**：将"留学"、"培训"、"研修"的机构当作毕业院校
       - **只有**页面明确提到"毕业于 XX 大学"、"XX 大学毕业"、"获得 XX 大学学位"时才能提取
       - 示例："美国俄亥俄州立大学博士后" → `null`（博士后不是学历教育）
       - 示例："哈佛大学访问学者" → `null`（访问学者不是学历教育）
       - 示例："曾赴美国贝勒医学院进修" → `null`（进修不是学历教育）
       - 示例："毕业于中山大学，获博士学位" → `"中山大学"`
     - 如果学校名称包含"大学 + 医学院/附属医院"格式，**只提取大学名称**
     - 示例：`"华中科技大学同济医学院"` → `"华中科技大学"`，`"中山大学附属第一医院"` → `"中山大学"`
6. **education（学历）必须从标准值中选择，需进行规范化映射**
   - 可选值：`博士研究生 `、`硕士研究生 `、`本科`、`大专`、` 中专`、` 高中`、` 初中及以下`
   - 映射示例：`"博士"` → `"博士研究生"`，`"硕士"` → `"硕士研究生"`，`"大学"` → `"本科"`，`"专科"` → `"大专"`
   - 🎓 **重要规则：如果页面显示多个学历信息，只提取最高学历**（博士 > 硕士 > 本科 > 大专 > 中专 > 高中 > 初中及以下）
   - ⚠️ **重要规则：只有在页面明确提到"学历"、"毕业于"、"学位"、"研究生"、"本科"、"专科"等关键词时才提取**
   - ❌ **禁止默认填充**：页面没有学历信息时，必须返回 `null`，绝对不能用"本科"填充
   - ❌ **禁止推测**：不要根据职称、职务、工作年限等信息推测学历
   - 🔍 **识别关键词**：`"学历"`、`"毕业于"`、`"学位"`、`"博士研究生"`、`"硕士研究生"`、`"本科"`、`"大专"`、`"中专"`、`"研究生"`
   - 📌 **示例**：
     - 页面提到"博士" → `"博士研究生"`
     - 页面提到"硕士研究生" → `"硕士研究生"`
     - 页面只提到"毕业于 XX 大学"但没说学历层次 → `null`（不要推测）
     - 页面没有提到任何学历信息 → `null`
7. **years_of_experience（从业年限）**
   - 💼 **从简介/擅长中提取工作年限**，提取数字
   - 💼 **支持中文数字转换**：`"二十年"` → `20`，`"三十年"` → `30`
   - 💼 **示例**：`"从事神经科临床工作 20 余年"` → `20`，`"从事超声诊断工作二十年"` → `20`，`"从业 30 年"` → `30`
   - 🧮 **如果提供的是参加工作的年份，需要计算从业年限**
   - 🧮 **计算方法**：用当前年份（2026）减去参加工作年份
   - 🧮 **示例**：`"1999 年参加工作"` → `2026 - 1999 = 27`，`"2000 年毕业"` → `2026 - 2000 = 26`
   - ⚠️ **重要规则**：
     - **优先提取明确的工作年限描述**（如"从事 XX 工作 XX 年"）
     - **如果同时有多个年限描述，选择与当前专业最相关的年限**
     - **如果提到"XX 余年"、"XX 多年"，按数字提取**（如"20 余年" → `20`）
     - **没有明确工作年限信息时返回 `null`**，不要推测
   - 📌 **示例**：
     - `"从事妇幼超声诊断工作十余年"` → `10`
     - `"工作二十五年"` → `25`
     - `"1996 年参加工作"` → `30`（2026-1996）
     - 没有提到工作年限 → `null`
8. **academic_title（学术头衔）必须从标准值中选择，需进行规范化映射**
   - 可选值：`教授 `、`副教授 `、`讲师`、` 助教`、` 研究员`、` 副研究员`、` 助理研究员`、` 研究实习员`
   - ⚠️ **重要规则：只有在页面明确提到"教授"、"副教授"、"研究员"等学术头衔时才提取**
   - ❌ **禁止推测**：不要根据职称、职务等信息推测学术头衔
   - ❌ **禁止默认填充**：页面没有学术头衔信息时，必须返回 `null`
   - ❌ **博导/硕导不是学术头衔**：`"博导"`、`"硕士生导师"`、`"研究生导师"` 不等于教授/副教授
   - 🔍 **识别关键词**：`"教授"`、`"副教授"`、`"研究员"`、`"副研究员"`、`"讲师"`、`"助教"`
   - 📌 **示例**：
     - 页面提到"教授" → `"教授"`
     - 页面提到"副教授" → `"副教授"`
     - 页面只提到"博导"、"研究生导师" → `null`（这不是学术头衔）
     - 页面没有提到任何学术头衔 → `null`
8. **administrative_position（行政职务）必须从标准值中选择，需进行规范化映射**
   - 可选值：`书记 `、`院长`、`局长 `、` 所长`、`经理 `、` 站长`、` 主任`、` 科长`、` 处长`、` 主管`、` 组长`、`病区护士长`、` 科护士长`、` 护士长`、` 住院部医生`、` 助理`、` 秘书`、`采购`、` 库管`、`普通职员`、` 地区分公司`、` 副书记`、` 副院长`、` 副局长`、` 副所长`、` 副经理`、` 副站长`、` 副主任`、` 副科长`、` 副处长`、` 病区副护士长`、` 科副护士长`、` 进修护师`、` 总住院医师`、` 护理部副主任`、`负责人`、` 支部书记`、` 主任助理`、` 带组医生`、` 所有权`、` 办公室主任`、` 床位医生`、`进修医生`、` 院长助理`、` 进修护士`、` 副护士长`
   - 映射规则：优先匹配副职（如"副院长"），无匹配时用原文
   - ⚠️ **验证规则**：
     - ✅ 有效：`"xx 医院院长"`、`"xx 医院心内科主任"`、`"本院护理部副主任"`、`"xx 医院护士长"`、`"检验科 xx 组组长"`、`"xxICU 护士长"`
     - ✅ 有效：**科室内部的行政职务**（如"组长"、"秘书"、"护士长"等）
     - ❌ 无效：`"中华医学会 xx 分会委员"`（学术组织）、`"曾任 xx 医院院长"`（历史职务）、`"xx 大学医学院院长"`（非本医院）、`"xx 医院（分院/帮扶医院）主任"`（非本院）、`"xx 研究所所长"`（科研机构）、`"xx 研究中心主任"`（科研机构）
   - 📌 **重要规则**：
     - 只提取在当前医院的行政职务
     - 当有多个职务时（如"现任 A 职务/B 医院职务"），**优先选择当前医院的职务**
     - **优先提取科室内部的行政职务（如组长、主管、秘书等）**，而非学术组织的委员/理事等职务
     - **"科室 + 护士长"格式（如"EICU 护士长"、"NICU 护士长"）应提取为"护士长"**
     - **科研机构职务（如"xx 研究所所长"、"xx 研究中心主任"、"xx 实验室主任"）应返回 null**
     - **学术组织职务（如"xx 学会委员"、"xx 协会常委"）应返回 null**
     - 示例：`"现任综合临床片科护士长/广东省人民医院河源医院护理部主任"` → `"护士长"`
     - 示例：`"检验科流式细胞组组长"` → `"组长"`
     - 示例：`"广东省人民医院 EICU 护士长"` → `"护士长"`
     - 示例：`"广东省神经科学研究所所长"` → `null`（科研机构职务，非医院行政职务）
     - 示例：`"中国医师协会微无创医学专业委员会单孔与腹腔镜学组委员"` → `null`（学术组织职务）
     - 示例：没有提到任何行政职务 → `null`
9. **title（职称）必须从标准值中选择，需进行规范化映射**
   - 临床医师：`主任医师 `、`副主任医师 `、` 主治医师`、` 主管医师`、` 医师`、` 医士`、`住院医师`、` 助理医师`
   - 药师：`主任药师 `、`副主任药师 `、`主管药师`、`药师`、`药士`
   - 护师：`主任护师 `、` 副主任护师`、` 主管护师`、` 护师`、`护士`
   - 技师：`主任技师 `、` 副主任技师`、` 主管技师`、`技师`、` 技士`
   - 中医：`主任中医师 `、` 副主任中医师`、` 主治中医师`、` 主管中医师`、` 中医师`、` 中医士`、` 住院中医师`
   - 中药：`主任中药师 `、` 副主任中药师`、` 主管中药师`、` 中药师`、`中药士`
   - 其他：`康复师 `、`营养师`、` 心理咨询师`、` 康复治疗师`、` 按摩师`、` 检验师`、` 麻醉师`、` 药剂师`、`心理治疗师` 等
   - 映射规则：
     - **优先匹配最具体的职称**（如"主任药师"优于"主任"）
     - **"主管技师"、"主管护师"等是独立职称，不要映射到"主治医师"**
     - **"心理治疗师"是独立职称，不要映射到"心理咨询师"**
     - 无匹配时用原文，但必须与医疗相关
   - ⚠️ **重要规则**：
     - 职称必须从页面明确提到的信息中提取
     - 不要根据学历、工作年限等信息推测职称
     - 页面没有提到职称时返回 `null`

**数据核验步骤：**
- 确认只提取了一个医生的信息
- 核对医生姓名与页面标题一致
- 验证数据完整性（姓名、科室、职称必填）
- **核验各字段的准确性**：
  - ✅ `alma_mater`：只有明确的"毕业于"信息才能提取，访问学者/博士后/进修不算
  - ✅ `education`：只有明确提到学历信息才能提取，没有时返回 `null`
  - ✅ `academic_title`：只有明确提到教授/研究员等才能提取，博导/硕导不算
  - ✅ `years_of_experience`：优先提取明确的工作年限描述
  - ✅ `administrative_position`：只提取当前医院的行政职务，学术组织/科研机构职务返回 `null`
  - ✅ `title`：从标准值中选择，没有明确职称时返回 `null`
  - ✅ `intro`/`specialty`：提取完整原文，不要省略
- 推送到 RabbitMQ 队列

---

## 二、推送数据格式

```json
{
  "name": "医生姓名（必须与页面标题一致）",
  "hospital": "医院全称",
  "standard_department": "标准科室名",
  "display_department": "展示科室名",
  "title": "职称（主任医师/副主任医师/主治医师/主管医师/医师/医士/住院医师/助理医师/主任药师/副主任药师/主管药师/药师/药士/主任护师/副主任护师/主管护师/护师/护士/主任技师/副主任技师/主管技师/技师/技士/主任麻醉师/副主任麻醉师/主管麻醉师/麻醉师/主任检验技师/副主任检验技师/检验技师/检验技士/主管检验技师/主任康复师/副主任康复师/主管康复师/康复师/康复治疗师/心理咨询师/主任检验医师/副主任检验医师/主治检验医师/检验医师/主管检验医师/高级技师/高级按摩师/中级按摩师/初级按摩师/初级营养师/主管营养师/副主任营养师/主任营养师/主任中医师/副主任中医师/主治中医师/主管中医师/中医师/中医士/住院中医师/主任中药师/副主任中药师/主管中药师/中药师/中药士/主管药剂师/主任药剂师/副主任药剂师）",
  "administrative_position": "行政职务（书记/院长/局长/所长/经理/站长/主任/科长/处长/主管/组长/病区护士长/科护士长/护士长/住院部医生/助理/秘书/采购/库管/普通职员/地区分公司/副书记/副院长/副局长/副所长/副经理/副站长/副主任/副科长/副处长/病区副护士长/科副护士长/进修护师/总住院医师/护理部副主任/负责人/支部书记/主任助理/带组医生/所有权/办公室主任/床位医生/进修医生/院长助理/进修护士/副护士长）",
  "academic_title": "学术头衔（教授/副教授/讲师/助教/研究员/副研究员/助理研究员/研究实习员）",
  "education": "学历（博士研究生/硕士研究生/本科/大专/中专/高中/初中及以下）",
  "degree": "学位",
  "alma_mater": "毕业院校（仅学校名称，不含院系/专业）",
  "years_of_experience": 28,
  "intro": "医生简介原文",
  "specialty": "擅长领域原文",
  "confidence_score": 100.0,
  "otherpropertys": "{\"confidence_name\": \"官网\", \"source_kind\": \"official\", \"seed_url\": \"官网首页 URL\", \"root_domain\": \"域名\", \"url\": \"医生详情页 URL\", \"match_score\": 1.0, \"page_record_time\": null, \"fetch_method\": \"playwright\"}",
  "source_model": "9S"
}
```

**数据核验规则：**

1. **唯一性验证**：只提取一个医生的信息
2. **一致性验证**：提取的医生姓名必须与页面标题中的姓名一致
3. **完整性验证**：至少包含姓名、科室、职称三个必填字段
4. **来源验证**：只从页面主体内容区域提取，忽略推荐/相关医生模块

### 核验失败的处理：
- 如果页面包含多个医生且无法确定目标医生：输出警告并跳过
- 如果提取的医生信息与 URL 不对应：重新分析页面结构
- 如果页面不是详情页（是列表页）：输出错误并不推送

---

## 三、队列配置

```
RABBITMQ_URL: amqp://guest:147258369aB@r.pisiewang.top:45672/
QUEUE_NAME: DoctorResult
```

**重要说明：**
- 每个医生信息必须立即推送到 DoctorResult 队列
- 推送前验证数据完整性和唯一性
- 只推送目标 URL 对应的医生信息
"""


copaw_client: Optional[CoPawClient] = None
processed_count = 0
my_consumer_id = f"scraper_{uuid.uuid4().hex[:8]}"


def process_doctor(url):
    global copaw_client, processed_count

    if copaw_client is None:
        print("❌ CoPaw client 未初始化")
        return

    sys.stdout.flush()

    prompt = PROMPT_TEMPLATE.replace("__Doctor_URL__", url)
    prompt_preview = prompt[:200].replace("\n", " ")
    print(f"📝 Prompt 预览: {prompt_preview}...")
    print(f"📏 Prompt 总长度: {len(prompt)} 字符\n")
    sys.stdout.flush()

    start_time = time.time()
    tool_calls = 0
    content_length = 0
    errors = []

    try:
        print("🤖 发送任务到 CoPaw Agent...")
        print(f"   API 地址：{copaw_client.config.base_url}")
        print(f"   超时时间：{copaw_client.config.timeout} 秒")
        print("-" * 70)
        sys.stdout.flush()

        events = copaw_client.chat(prompt)

        print("\n📥 接收 CoPaw 响应:\n")
        sys.stdout.flush()

        for event in events:
            event_type = event.get("type", "unknown")

            if event_type == "thinking_start":
                msg_id = event.get("data", {}).get("msg_id")
                print(f"\n💭 Agent 开始思考... (msg_id: {msg_id})")
                sys.stdout.flush()

            elif event_type == "content_delta":
                text = event.get("data", {}).get("delta", "")
                if text:
                    print(text, end="", flush=True)
                    content_length += len(text)

            elif event_type == "tool_call":
                tool_calls += 1
                tool_data = event.get("data", {})
                tool_name = tool_data.get("name", "unknown")
                tool_args = tool_data.get("arguments", {})
                print(f"\n\n🔧 [{tool_calls}] 工具调用：{tool_name}")
                if tool_args:
                    print(f"   参数：{json.dumps(tool_args, ensure_ascii=False)[:200]}")
                sys.stdout.flush()

            elif event_type == "tool_result":
                tool_data = event.get("data", {})
                result_preview = json.dumps(tool_data, ensure_ascii=False)[:200]
                print(f"   ✅ 工具结果：{result_preview}...\n")
                sys.stdout.flush()

            elif event_type == "error":
                error_msg = event.get("data", "未知错误")
                errors.append(error_msg)
                print(f"\n❌ 错误：{error_msg}")
                sys.stdout.flush()

            elif event_type == "response":
                status = event.get("data", {}).get("status", "unknown")
                if status == "completed":
                    print("\n\n✅ Agent 响应完成 - 准备消费下一个消息")
                else:
                    print(f"\n📊 响应状态：{status}")
                sys.stdout.flush()

        elapsed_time = time.time() - start_time

        print("\n" + "=" * 70)
        print(f"📊 任务统计:")
        print(f"  - 处理时间：{elapsed_time:.2f} 秒")
        print(f"  - 输出长度：{content_length} 字符")
        print(f"  - 工具调用：{tool_calls} 次")
        print(f"  - 错误数量：{len(errors)} 次")
        print("=" * 70)
        print("\n✅ 当前消息处理完成，等待下一个消息...\n")
        sys.stdout.flush()
    except Exception as e:
        print(f"\n❌ 处理过程出错：{e}")
        errors.append(str(e))


def main():
    global copaw_client, processed_count

    # 解析命令行参数
    parser = argparse.ArgumentParser(
        description="医生信息提取器 - 从数据库读取 URL 并提取医生信息"
    )
    parser.add_argument(
        "--copaw-url",
        type=str,
        default="http://localhost:8088",
        help="CoPaw API 地址 (默认：http://localhost:8088)",
    )
    parser.add_argument(
        "--shard-index",
        type=int,
        default=0,
        help="当前节点的分片索引 (从 0 开始，默认：0)",
    )
    parser.add_argument(
        "--shard-total",
        type=int,
        default=1,
        help="总分片数量 (默认：1，表示不分片)",
    )
    args = parser.parse_args()

    copaw_base_url = args.copaw_url
    shard_index = args.shard_index
    shard_total = args.shard_total

    if shard_index < 0 or shard_index >= shard_total:
        print(f"❌ 分片索引无效：shard_index={shard_index}, shard_total={shard_total}")
        print(f"   shard_index 必须满足：0 <= shard_index < shard_total")
        sys.exit(1)

    config = CoPawConfig(base_url=copaw_base_url, timeout=3600.0)
    copaw_client = CoPawClient(config)

    print("\n" + "=" * 70)
    print("🚀 医生信息提取器启动")
    print("=" * 70)
    print(f"📍 CoPaw API: {copaw_base_url}")
    print(f"📊 分片配置：节点 {shard_index} / 共 {shard_total} 节点")
    print(f"🎯 处理范围：id % {shard_total} = {shard_index}")
    print("=" * 70)
    sys.stdout.flush()

    print("\n🔍 检查 CoPaw API 连接...")
    sys.stdout.flush()

    if not copaw_client.health_check():
        print(f"\n❌ 无法连接到 CoPaw API: {copaw_base_url}")
        print("   请检查:")
        print("   1. CoPaw 服务是否运行")
        print("   2. API 地址是否正确")
        print("   3. 网络连接是否正常")
        sys.exit(1)

    print("✅ CoPaw API 连接成功\n")
    sys.stdout.flush()

    print("\n🔍 检查 CoPaw API 连接...")
    sys.stdout.flush()

    if not copaw_client.health_check():
        print(f"\n❌ 无法连接到 CoPaw API: {copaw_base_url}")
        print("   请检查:")
        print("   1. CoPaw 服务是否运行")
        print("   2. API 地址是否正确")
        print("   3. 网络连接是否正常")
        sys.exit(1)

    print("✅ CoPaw API 连接成功\n")
    sys.stdout.flush()

    retry_count = 0

    conn = psycopg2.connect(
        host="r.pisiewang.top",
        port="54321",
        database="datafresh",
        user="postgres",
        password="j2UiYbIsDm3TZMP2",
    )
    cursor = conn.cursor()
    cursor.execute(
        f"""
        SELECT * FROM datafresh.doctor_search 
        WHERE source_model <> '9S' 
          AND CAST(otherpropertys AS json) ->>'url' is not null 
          and hospital like '%广东省人民%'
          AND id % {shard_total} = {shard_index}
        """
    )
    rows = cursor.fetchall()

    if cursor.description is None:
        print("❌ 查询未返回结果集")
        conn.close()
        return

    column_names = [desc[0] for desc in cursor.description]

    for row in rows:
        row_dict = dict(zip(column_names, row))
        otherpropertys = row_dict.get("otherpropertys")

        if otherpropertys:
            try:
                otherpropertys = json.loads(otherpropertys)
                process_doctor(otherpropertys["url"])

            except json.JSONDecodeError as e:
                print(f"❌ JSON 解析失败：{e}")
                print(f"   原始数据：{otherpropertys[:200]}...")
        else:
            print(f"⚠️ otherpropertys 为空")

        print("-" * 70)
        sys.stdout.flush()

    conn.close()
    print(f"成功读取 {len(rows)} 条记录")
    sys.stdout.flush()


if __name__ == "__main__":
    main()
